# 三衍协作审计协议：从规则文本到可验证硬门

## 摘要

多 Agent 协作的常见问题不是缺少角色名称，而是缺少可验证的交接合同。执行者可以跳过步骤，审阅者可能复述执行者的结论，引用可能只是格式正确却指向不存在的行，高风险动作也可能没有真正的人类授权。

本项目选择一个很小的工程目标：把协作记录中最基础的五项要求做成确定性硬门，并诚实保留机器无法证明的部分。

## 最小模型

一次审计记录由五类信息组成：

1. **角色**：谁执行，谁审阅；
2. **状态**：Observe → Orient → Decide → Act → Reflect → Persist；
3. **证据**：事实性声明对应哪个 `file:line`；
4. **权限**：高风险任务是否经过人类批准；
5. **收尾**：结果与停止条件是什么。

审计器不调用模型，也不修改被审计工作区。任何硬门失败都返回 `FAIL`，从而让失败可复现，而不是依赖另一个 Agent 的主观打分。

## 为什么只做这五项

“AI 是否真正理解”“审阅者是否完全独立”“引用是否语义蕴含结论”都很重要，但当前小工具无法诚实证明。把这些问题伪装成一个总分会制造虚假安全感。因此协议把结果分成两层：

- **确定性层**：结构、路径、行存在性、角色标签、权限字段和收尾字段；
- **人工层**：证据质量、语义正确性、风险判断、身份真实性和最终决策。

## 与 Graph Engineering 的关系

执行者、审阅者和人类批准者可以看作三个节点；审计记录是它们之间流动的状态；硬门是确定性路由条件。读和评议可以并行，但对真实工作区的写入仍应保持单点所有权。

## 历史快照，不是当前证据

2026-05-29 的内部研究文档曾记录“26+ 次审计”“11000+ 代引擎运行”“12/14 项工程资产”等数字，也提出 OODA、错误模式库、失败仪式、外部视角和质量分等概念。这些材料推动了本项目，但本公开仓库目前没有足够原始证据复验那些数字，也没有公开全部内部资产。

因此这些数字不再作为当前能力声明。当前可复验的证据只有本仓库中的源码、合成示例、测试和 CI 配置。

## 停止线

以下任一情况都不应被自动“修到通过”：

- 引用越过授权根目录；
- 引用文件或行不存在，或目标行为空；
- 执行者与审阅者相同；
- 六阶段缺失或乱序；
- 高风险任务未记录人工批准；
- 没有明确结果或停止条件。

正确处理方式是保留失败报告，修正真实流程或输入，再发起新的审计。
